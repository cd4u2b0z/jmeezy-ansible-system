---
# CachyOS already has these services enabled/configured
# Commenting out to avoid conflicts

system_services: []
  # NetworkManager - already enabled by CachyOS
  # bluetooth - no hardware
  # ufw - already configured by CachyOS
  # coolercontrold - not installed
  # cronie - not needed

# Timers to enable
system_timers: []
  # reflector.timer - CachyOS uses cachyos-rate-mirrors instead

# User services to enable (after dotfiles)
user_services:
  - hypridle.service
  # pipewire - already running via CachyOS
  # wireplumber - already running via CachyOS

# User timers to enable
user_timers: []
  # update-cache.timer - optional
